# bad distance arg errors for cnn

    Code
      cnn(circle_numeric, var = "class", distance = "minkowski")
    Condition
      Error in `cnn()`:
      ! `distance` must be one of "euclidean", "cosine", "mahalanobis", "manhattan", or "chebyshev", not "minkowski".

